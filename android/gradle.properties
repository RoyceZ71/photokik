# Project-wide Gradle settings.

# Specifies the JVM arguments used for the daemon process.
org.gradle.jvmargs=-Xmx2048M -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
org.gradle.parallel=true

# AndroidX package structure
android.useAndroidX=true

# Enables namespacing of each library's R class
android.nonTransitiveRClass=true

# Automatically convert third-party libraries to use AndroidX
android.enableJetifier=true

# Enable Gradle build cache
org.gradle.caching=true

# Enable Gradle daemon
org.gradle.daemon=true

# Warning mode
org.gradle.warning.mode=summary

# Kotlin code style
kotlin.code.style=official

# Uncomment the line below that matches your JDK installation:

# Windows - Set your actual JDK path (JAVA_HOME should point to JDK root, not bin folder)
org.gradle.java.home=C:/Program Files/Java/jdk-17

# macOS - Common JDK locations (uncomment if needed):
# org.gradle.java.home=/Library/Java/JavaVirtualMachines/jdk-17.jdk/Contents/Home

# Linux - Common JDK locations (uncomment if needed):
# org.gradle.java.home=/usr/lib/jvm/java-17-openjdk-amd64

# Note: JAVA_HOME should be the root JDK directory (not the bin subfolder)
